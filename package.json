{
  "name": "xbpneus-racing",
  "version": "3.6.1",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": "22.18.0",
    "pnpm": "10.15.1"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000 --strictPort",
    "assets:generate": "node scripts/generate-assets.mjs",
    "build": "corepack pnpm run assets:generate && vite build && node scripts/copy-standalone-server.mjs && node scripts/dist-integrity.mjs --write",
    "start": "node dist/standalone-server.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "check": "tsc --noEmit && tsc -p tsconfig.node.json --noEmit",
    "lint": "corepack pnpm run check && prettier --check package.json pnpm-workspace.yaml vite.config.ts vitest.config.ts server client/index.html client/public client/src tests security scripts .github render.yaml",
    "format": "prettier --write package.json pnpm-workspace.yaml vite.config.ts vitest.config.ts server client/index.html client/public client/src tests security scripts .github render.yaml",
    "test": "vitest run && node scripts/run-node-tests.mjs",
    "audit": "corepack pnpm audit --audit-level low",
    "release:check": "node scripts/release-check.mjs",
    "verify": "corepack pnpm run lint && corepack pnpm run build && corepack pnpm run test && corepack pnpm run dist:integrity && corepack pnpm run release:check && corepack pnpm run verify:offline",
    "verify:offline": "node scripts/verify-offline-release.mjs",
    "dist:integrity": "node scripts/dist-integrity.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "9.20.0",
    "@babylonjs/loaders": "9.20.0",
    "lucide-react": "0.453.0",
    "react": "19.2.1",
    "react-dom": "19.2.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "4.1.14",
    "@types/node": "22.18.0",
    "@types/react": "19.2.1",
    "@types/react-dom": "19.2.1",
    "@vitejs/plugin-react": "5.0.4",
    "esbuild": "0.28.1",
    "prettier": "3.6.2",
    "tailwindcss": "4.1.14",
    "typescript": "5.6.3",
    "vite": "7.3.5",
    "vitest": "4.1.10"
  },
  "packageManager": "pnpm@10.15.1"
}
